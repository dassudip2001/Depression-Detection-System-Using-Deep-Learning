apiVersion: v1
kind: Pod
metadata:
  name: twitter
  labels:
    name: twitter
spec:
  containers:
  - name: twitter
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 5000
